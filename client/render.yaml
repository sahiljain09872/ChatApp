services:
  - type: web
    name: my-react-app
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: client/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
